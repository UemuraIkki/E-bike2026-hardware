# 個人情報を登録しないための運用規則

このリポジトリは公開状態を前提に扱います。次の情報はコミットしません。

## 登録禁止

- 氏名、学籍番号、個人メールアドレス、電話番号
- 実名と `担当A`〜`担当D` の対応表
- Moodleのユーザ名、画面、URL中の個人識別情報
- 顔、名札、制服の刺繍、学生証が判別できる写真
- 位置情報を含む画像メタデータ
- APIキー、パスワード、アクセストークン、秘密鍵
- PCの個人用ホームディレクトリを含む設定ファイル
- 表紙、個人の感想、最終提出用Word/PDF
- チャット・メール・会議ログの原文、参加者一覧、送信者名
- 体調、出欠、宿泊、食事、私的予定など、技術報告に不要な生活情報
- 学校クラウドの個人用共有URLや、URL中のアカウント識別子

GitHubアカウント名および `UemuraIkki`・`UE_e` を含むローカルパス表記は、プロジェクト管理上の許容識別子とします。ただし、設定ファイルに絶対パスを保存する必要はないため、`settings_wb6x.json` は追跡しません。

## 匿名担当ラベル

- `担当A`: モータ制御、モータ位置検出用Hall、プロジェクト管理、車速Hall・CAN両担当の補助
- `担当B`: 車速検出用Hallセンサ関係
- `担当C`: 車体整備・機械加工関係
- `担当D`: CAN通信モジュール関係

実名への置換は、提出直前にGitHub外のローカルコピーで行います。

班内連絡ログを根拠として使う場合は、原文をコミットせず、日付、匿名担当、技術的な作業、確認結果だけを要約します。画像・動画と書かれていても添付本体を確認できない場合は、内容を推測せず「原本回収待ち」とします。

## 写真を追加する前の確認

1. 顔、名札、学生証、PC画面、荷物の宛名を写さない。
2. 画像からGPS、撮影者、端末名などのメタデータを削除する。
3. 背景にホワイトボード、名簿、Moodle画面がないか確認する。
4. ファイル名に氏名や学籍番号を使わない。
5. `docs/evidence/photo-register.md` に匿名の内容説明だけを書く。

未処理写真は `raw-media/` に置きます。このディレクトリは `.gitignore` で除外します。

## コミット前確認

```powershell
rg -n --hidden -g '!.git/**' '(メールドメイン|学籍番号の形式|BEGIN .*PRIVATE KEY|api[_-]?key|password|token)'
git diff --cached
```

検索語は実際の学校メールドメインや学籍番号形式に置き換えます。検出結果をそのままチャットやIssueへ貼り付けないでください。
